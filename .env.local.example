# Public URL — used by the QR code. If unset, the QR encodes whatever
# `window.location.origin` is at runtime (which is fine for localhost +
# most deployments).
# VITE_PUBLIC_URL=https://your-talk.example
